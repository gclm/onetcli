interface:
  display_name: "Onetcli"
  short_description: "Repo-specific patterns and workflows for onetcli"
  default_prompt: "Use the onetcli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true